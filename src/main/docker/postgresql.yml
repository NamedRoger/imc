version: '2'
services:
  imc-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/IMC/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=IMC
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
